<style>
  .content {
            display: none;
            /* Other styles for the content */
            justify-content: center;
            align-items: center;
            width: 100%;
            height: 100%;
        }

        .loader-wrapper {
            /* Styles for the loader */
            transition: opacity 1s ease-in-out;
            /* CSS transition for opacity */
            width: 100%;
            height: 100%;
            position: absolute;
            top: 0;
            left: 0;
            background-color: #ffffff;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .loader-wrapper.hide {
            opacity: 0;
            pointer-events: none;
            /* Disable pointer events when hidden */
        }
</style>
<script>
  document.addEventListener('DOMContentLoaded', function () {
      var content = document.querySelector('.content');
      var loader = document.querySelector('.loader-wrapper');

      setTimeout(function () {
          loader.classList.add('hide'); // Add the 'hide' class to trigger the fade-out transition
          content.style.display = 'block';
      }, 1500); // Delay in milliseconds (3 seconds in this example)
  });
</script>
<div class="content">
  <% if(recipe != null ) { %>

    <nav aria-label="breadcrumb">
      <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="/">Home</a></li>
        <li class="breadcrumb-item active" aria-current="page"><%- recipe.name %></li>
      </ol>
    </nav>
  
  
    <div class="row">
  
      <div class="col-12 col-md-4">
        <img src="/uploads/<%- recipe.image %>" class="img-fluid sticky-top" style="top: 20px;" alt="<%- recipe.name %>" loading="lazy">
      </div>
  
      <div class="col-12 col-md-8">
        <div class="row">
          <div class="col-12"><h1><%- recipe.name %></h1></div>
          <div class="col-12 mb-4"><i class="bi bi-tag"></i> <%- recipe.category %></div>
          <div class="col-12" style="white-space: pre-line;">
            <h4>Cooking Instructions</h4>
            <%- recipe.description %>
          </div>
        </div>
  
        <div class="row pt-4">
          <div class="col-12">
            <h4>Ingredients</h4>
            <ul class="list-group list-group-flush">
              <% recipe.ingredients.forEach(function(ingredients, index){ %>
                <li class="list-group-item"><%= ingredients %></li>
              <% }) %>
            </ul> 
          </div>
        </div>
        <a href="/viewcart.html">Add To Cart</a>
        <!-- <button type="submit" > Add To Cart</button> -->
      </div>
      </div>
    
    </div>
  <% } else { %>
    <p>No item found.</p>
  <% } %>

</div>

<div class="loader-wrapper">
  <img src="https://media2.giphy.com/media/6rmxsMnN0kSryPsI9p/giphy.gif" alt="image raledhu ra pumka !!">
</div>